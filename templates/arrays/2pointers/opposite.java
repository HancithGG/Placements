//components Array/String + Need information from both ends + Move inward = Opposite Two Pointers

int left = 0;
int right = arr.length - 1;

while(left < right){

    // logic

    left++;
    right--;
}
